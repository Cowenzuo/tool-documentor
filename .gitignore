# dependencies
node_modules/

# build outputs（库 dist 与桌面 out 均为编译产物，clone 后由 dev/build 自动重建）
out/
dist/
release/
*.tsbuildinfo

# 临时/探针产物
.e2e-workspace/
.probe/
.tmp*
*~
# 统一临时目录（E2E 工作区、冒烟导出、打包调试等；不放系统临时目录）
temp/

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editors / os
.vscode/
.idea/
.DS_Store
Thumbs.db

# local
*.local

# 本地模板资产（438C 等，版权分发边界：不入库）
localtest/
