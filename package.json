{
  "name": "tool-rwdoc",
  "version": "0.1.0-alpha1",
  "private": true,
  "license": "MIT",
  "description": "Documentor（模板驱动的结构化 DOCX 文档编辑器）Node.js + Electron 重写版",
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=11"
  },
  "packageManager": "pnpm@11.22.0",
  "devDependencies": {
    "typescript": "~5.9.3"
  },
  "scripts": {
    "build:libs": "pnpm --filter @documentor/core build && pnpm --filter @documentor/templates build && pnpm --filter @documentor/postprocess build && pnpm --filter @documentor/docx build",
    "dev": "pnpm build:libs && pnpm --filter @documentor/desktop dev",
    "build": "pnpm build:libs && pnpm --filter @documentor/desktop build",
    "start": "pnpm --filter @documentor/desktop start",
    "typecheck": "pnpm -r typecheck",
    "cli:test-export": "pnpm --filter @documentor/desktop cli:test-export"
  }
}
