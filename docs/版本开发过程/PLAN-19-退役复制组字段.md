# PLAN-19 退役复制组字段

> 状态：已实施。把 `copyGroupId` 这个字段从产品、工具、文档与模板里清掉。
> 它从旧版数据模型里一路搬过来，却一处行为都没有：留着只会让人以为它在管事。

## 1 它是什么，为什么退役

`copyGroupId` 是旧版产品留下的**重复单元标记**：同一父节点下同组的节点算"一份重复单元的多份"，
作者用它标 `module-group`、`key-tech`、`perf-target-func` 这类位置。重写版把它整个搬了过来：

| 层 | 现状（退役前） |
| --- | --- |
| 模板 JSON | 5 套结构模板 24 处在用 |
| 文档树 | `DocumentNode.copyGroupId`，深拷贝与快照照抄 |
| 库表 | `node.copy_group_id` 列 |
| 通道 | `NodeDto.copyGroupId` |
| 校验 | 一条规则：`copyGroupId 在，copyable 不是 true · 复制不了` |
| 行为 | **一处都没有**：界面不显示、复制不看它、导出不看它；`copyGroupCount()` 零调用方，死代码 |

复制这件事本身早就闭环了，闭环的口径是"一章一份、原样复制"（PLAN-18）：复制只看 `copyable`
与整棵子树，从来不问组。所以这个字段既不是复制机制的一部分，也没有任何别的用处。

## 2 改了什么

| 位置 | 改动 |
| --- | --- |
| `packages/core/src/tree.ts` | 去掉字段与 `copyGroupCount()`，深拷贝/快照/恢复三处赋值一并去掉 |
| `packages/core/src/store.ts` | 建表不再有这一列；读写两侧都不再碰它 |
| `packages/templates/src/types.ts`、`manager.ts` | 节点定义去掉字段，解析与实例化各一处 |
| `packages/templates/src/validate.ts`、渲染层 `templateValidate.ts` | 去掉 `node.copyGroup.notCopyable` 这条规则（两处同源） |
| `packages/desktop/src/shared/project.ts`、`main/services/dto.ts` | DTO 去掉字段 |
| 模板编辑器注释 | 提到"复制组"的三处注释一并清掉 |
| 工程仓库 `tools/view-project.cjs`、`apply-project.cjs` | 不再打印 `组:xxx`，规格里也不再认这个字段 |
| 工程仓库 `docs/02`、`03`、`11` | 字段表、建表语句、命令示例与那份工程化记录里的"复制组"一行/一句清掉 |
| 模板仓库 5 份结构模板 | 24 处 `"copyGroupId": "…"` 删掉（改前整份备份，改后逐份核对：除该键外深度相等、uuid 不变、行数正好少那么多） |
| 七份真实工程库 | `node` 表整列 `ALTER TABLE node DROP COLUMN copy_group_id`（改前整目录备份，改后逐项核对，见第 3 节） |

## 3 老数据怎么办

- **模板里的 24 处已经清掉**：模板是"以后新建工程"的来源，留着它会继续往新工程里写。
- **工程库里的那一列也删掉了**：七份真实工程的 `node` 表整列删掉，改前每份工程整目录备份，
  改后逐项核对：保留下来的列逐值不变、节点与内容块行数不变、产品照常打开且模板解得出来、
  这一列确实不在了。七份全过，零问题。
- **旧包不再兼容，办法是重打包**：`0.1.2-alpha1` 那个已经打出去的包，读语句里还有这一列，
  删列之后它打不开这七份工程，会报缺列。这是明知故犯的取舍：字段一处行为都没有，
  留着只是为了给旧包让路，而那个包是内部版本。补救是**同版本号重打一次新包**，
  不是把列加回去——把列加回去等于让一个空转的字段继续锁住数据模型。
- 字段退役了，但**"重复单元"这个写作概念还在**（作者心里的一节一份）。哪天要在编辑侧做成能力
  （例如"再加一个模块组"一次复制整份单元），那是新方案，字段另起，不复用这个已经空转的名字。

## 4 与既有文档的关系

PLAN-16 第 5 节把它列成"还没做的三件"之一（去留未定）。本文收口：**退**。
PLAN-13 第 6 节、PLAN-16 第 5 节里关于它的那两行以本文为准。

## 5 验收

1. 产品里搜不到 `copyGroupId`／`copy_group_id`／`复制组`；
2. 模板与样式照常加载：结构 6 份、样式 4 份、零跳过零警告；
3. 模板编辑器的校验不再报这一条，其余规则一条不少；
4. 既有八份自检全绿，`pnpm typecheck` 通过；
5. 七份真实工程删列后逐项核对通过，重打的同版本号新包能打开它们。
